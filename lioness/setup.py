from setuptools import setup, find_packages

setup(
    name="lioness",
    version="0.1.0",
    description="Code for the LIONESS project",
    packages=["lioness", "lioness.segmentation", "lioness.utils"],
)
